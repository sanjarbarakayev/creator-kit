# Testing

Use the **tdd-guide** agent for TDD workflow and the **e2e-runner** agent for Playwright tests.

- Framework: Vitest
- Minimum coverage: 80% (branches, functions, lines)
- TDD: Write test first (RED), implement (GREEN), refactor (IMPROVE)
- Run: `pnpm test` or `pnpm vitest run --coverage`
